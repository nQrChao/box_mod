<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="applet"
            type="com.box.common.data.model.ModInfo" />

    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/icon"
            android:paddingHorizontal="@dimen/idp_8"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/idp_120"
            android:src="@drawable/mod_icon"
            android:scaleType="fitCenter"
            app:loadRoundSizeImage="@{applet.pic}" />

        <TextView
            android:id="@+id/name"
            android:paddingHorizontal="@dimen/idp_8"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textStyle="bold"
            android:text="@{applet.marketjson.app_title}"
            android:textColor="@color/black90"
            android:textSize="@dimen/isp_15" />

        <TextView
            android:id="@+id/desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/idp_8"
            android:paddingVertical="@dimen/idp_8"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{applet.marketjson.app_title2}"
            android:textColor="@color/text_more_dialog_color"
            android:textSize="@dimen/isp_14" />

    </LinearLayout>

</layout>