<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="com.box.other.blankj.utilcode.util.StringUtils" />

        <variable
            name="valuationCommitBean"
            type="com.box.common.data.model.ModValuationCommitBean" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="10">

        <TextView
            android:id="@+id/tv_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="3"
            android:gravity="end"
            android:paddingEnd="@dimen/idp_10"
            android:text="@{valuationCommitBean.title}"
            android:textColor="@color/text_default_color"
            android:textSize="@dimen/isp_12"
            tools:text="游戏名啊啊啊" />

        <TextView
            android:id="@+id/et_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="7"
            android:gravity="start"
            android:paddingHorizontal="@dimen/idp_10"
            android:paddingVertical="@dimen/idp_3"
            android:singleLine="true"
            android:text="@={valuationCommitBean.content}"
            android:textColor="@color/text_default_color"
            android:textSize="@dimen/isp_12"
            tools:text="测试" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="*"
            android:textColor="#FF3F3F"
            android:textSize="@dimen/mod_edit_title_text_size"
            android:visibility="gone" />


    </LinearLayout>

</layout>