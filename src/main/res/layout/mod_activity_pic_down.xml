<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.box.mod.ui.activity.ModActivityPicDownModel" />

        <variable
            name="click"
            type="com.box.mod.ui.activity.ModActivityPicDown.ProxyClick" />
    </data>

    <LinearLayout
        android:id="@+id/rootView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/idp_10">

            <ImageView
                android:id="@+id/appletPic"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/tv_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="@dimen/idp_15"
                android:background="@drawable/mod_btn_confirm_selector"
                android:onClick="@{()-> click.savePic()}"
                android:paddingHorizontal="@dimen/idp_18"
                android:paddingVertical="@dimen/idp_9"
                android:text="下载壁纸"
                android:textColor="@color/white90"
                android:textSize="@dimen/sp_14" />
        </FrameLayout>


    </LinearLayout>
</layout>