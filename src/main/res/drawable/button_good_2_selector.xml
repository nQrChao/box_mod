<?xml version="1.0" encoding="utf-8"?>
<!-- 默认按钮样式 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 禁用状态 -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners />
            <solid android:color="@color/common_button_disable_color" />
        </shape>
    </item>

    <!-- 按压状态 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:centerX="0.5"
                android:centerY="0.5"
                android:endColor="#AAFF3E01"
                android:gradientRadius="@dimen/idp_180"
                android:startColor="#AAFF8933"
                android:type="linear"
                android:useLevel="false" />
            <!--angle控制渐变的方向-->
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners/>
        </shape>
    </item>

    <!-- 焦点状态 -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:centerX="0.5"
                android:centerY="0.5"
                android:endColor="#AAFF3E01"
                android:gradientRadius="@dimen/idp_180"
                android:startColor="#AAFF8933"
                android:type="linear"
                android:useLevel="false" />
            <!--angle控制渐变的方向-->
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners />
        </shape>
    </item>

    <!-- 默认状态 -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:centerX="0.5"
                android:centerY="0.5"
                android:endColor="#FF3E01"
                android:gradientRadius="@dimen/idp_180"
                android:startColor="#FF8933"
                android:type="linear"
                android:useLevel="false" />
            <!--angle控制渐变的方向-->
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners/>
        </shape>
    </item>

</selector>